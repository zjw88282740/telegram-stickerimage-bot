name: Build and publish Docker image

on:
  push:
    branches: [master]
    paths:
      - 'bot.js'
      - 'lang/**'
      - 'package.json'
      - 'package-lock.json'
      - 'Dockerfile'
      - 'config.docker.js'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/telegram-stickerimage-bot:latest
            ghcr.io/${{ github.repository_owner }}/telegram-stickerimage-bot:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
